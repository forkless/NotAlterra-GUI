NotAlterra — Subnautica 2 Save Manager (Windows GUI)
Windows native application. No network access. No telemetry.

Portable `.exe` download. Requires Windows App SDK runtime.

See `CHANGELOG.md` for version history.
