# Testing & Fuzzing

## Test Suite

- **Framework:** xUnit
- **Location:** `src/NotAlterra.UI.Tests/`
- **Count:** 96 pass, 2 skip, 0 fail
- **Run:** `dotnet test src/NotAlterra.UI.Tests -c Debug`

## Test Categories

- **GvasParserTests** — GVAS metadata extraction (corrupt files, real files, edge cases)
- **AppConfigTests** — registry persistence, backup root, ensure dir
- **GuardTests** — game running detection (platform path handling)
- **SaveOpsTests** — backup/restore operations

## Fuzzing

- FsCheck property-based tests within the xUnit suite
- 16,000 iterations per property
- Random inputs for GVAS parser, BinaryReader, SaveOps, Guard

## Test Fixtures

- `gvas-files/` — synthetic .sav test files (generated by scripts/_gen_synthetic_saves.py)
- No .bak files in fixtures (only .sav)
- Corrupt files: magic, bool, playtime, truncated, zeroed
